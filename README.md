Vim files for working with the fab language.

To use the syntax file, add something like the following to your .vimrc:

    so ~/.vim/syntax/fab.vim
    au BufRead,BufNewFile *.fab set filetype=fab


